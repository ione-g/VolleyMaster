package eone.grim.volleymaster.utils.interfaces

import androidx.fragment.app.Fragment

interface OnTrainingSelectedListener {
    fun onTrainingSelected(trainingDetailFragment: Fragment, actionId: Int)

}